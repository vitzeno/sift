# Built CLI binary (see README's `go build -o sift ./cmd/sift`)
/sift
*.exe
*.test

# Generated pipeline output. examples/ and testdata/ only ever commit
# .sift/.csv fixtures; every .jsonl there is produced by running one of
# them (see Makefile's `clean` target for examples/; testdata/'s own
# output is cleaned up automatically by each test's t.Cleanup).
examples/*.jsonl
testdata/*.jsonl

# Coverage output
*.out

# OS cruft
.DS_Store

# Working notes kept out of the published repo.
/claude.md
/design/
